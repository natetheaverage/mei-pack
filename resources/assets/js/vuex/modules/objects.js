 import {
  //SET_OBJECT,
} from '../mutation-types'
import truth from '../../truth/truth.js'

// initial state
const state = {
  //menu: truth.menus
}

const mutations = {

  

}

export default {
  state,
  mutations
}