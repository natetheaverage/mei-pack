/**
 * 1. Avoid the IE 10-11 `min-height` bug.
 * 2. Set `flex-shrink` to `0` to prevent some browsers from
 *    letting these items shrink to smaller than their content's default
 *    minimum size. See http://bit.ly/1Mn35US for details.
 * 3. Use `%` instead of `vh` since `vh` is buggy in older mobile Safari.
 */

.app 
  display flex
  height 100vh /* 1, 3 */
  flex-direction column

.app-header
  z-index 1000
// .app-header,
// .app-footer 
//  flex none /* 2 */


.app-container 
  //min-height 100%
  display flex
  flex 1 0 auto /* 2 */
  flex-direction column
  padding 0


.app-content 
  margin-top $holygrail-space-full
  justify-content auto
  flex 1

.app-nav 
  order -1
  z-index 500

.app-aside
  position fixed
  right 0
  top 0
  padding-top 45px
  height 100%
  z-index 700

@media (min-width 768px) 
  .app-container 
    flex-direction row
  
  .app-content 
    flex 1
    padding 0 $holygrail-space-full
    margin 0
  
  // .app-nav * used for transition dont chagee
  //   flex 0 0 14em
  // .app-aside 
  //   flex 0 0 14em
